import { FC } from "react";
import { Background } from "./account/Account";

export const StaffAccounts: FC = () => {
  
  return (
    <Background>
      <div>staff accoutns approval page</div>
    </Background>
  );
};